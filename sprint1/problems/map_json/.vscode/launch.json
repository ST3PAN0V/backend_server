{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(gdb) LaunchDB",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/bin/game_server",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "CMake Build", // это должно соответствовать имени задачи в tasks.json
            "miDebuggerPath": "/usr/bin/gdb"
        }
    ]
}

